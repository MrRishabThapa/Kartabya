import { ArrowLeft, Sparkles } from 'lucide-react';
import type { TeachTopic } from '@/lib/games/teach/types';

export default function TopicPickerScreen({ topics, onSelect, onBack, onRandom }: { topics: TeachTopic[]; onSelect: (topic: TeachTopic) => void; onBack: () => void; onRandom: () => void }) {
  const groups = topics.reduce<Record<string, TeachTopic[]>>((all, topic) => ({ ...all, [topic.subject]: [...(all[topic.subject] ?? []), topic] }), {});
  return <div><button type="button" onClick={onBack} className="mb-5 inline-flex items-center gap-2 rounded-lg px-2 py-2 text-sm font-bold text-slate-500 hover:bg-white"><ArrowLeft size={17} /> Back</button><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-brand-primary">Choose your target</p><h2 className="mt-1 text-2xl font-black text-slate-800">What do you want to teach?</h2></div><button type="button" onClick={onRandom} className="rounded-xl border border-brand-primary-tint bg-brand-primary-bg p-2 text-brand-primary" aria-label="Choose random topic"><Sparkles size={18} /></button></div><div className="mt-6 space-y-5">{Object.entries(groups).map(([subject, items]) => <section key={subject}><h3 className="mb-2 text-xs font-extrabold uppercase tracking-[0.14em] text-slate-400">{subject}</h3><div className="grid gap-3">{items.map((topic) => <button type="button" key={topic.id} onClick={() => onSelect(topic)} className="rounded-2xl border border-slate-200 bg-white p-4 text-left transition hover:-translate-y-0.5 hover:border-brand-primary-light hover:shadow-md"><div className="flex items-start justify-between gap-3"><div><h4 className="font-extrabold text-slate-800">{topic.title}</h4><p className="mt-1 text-sm leading-5 text-slate-500">{topic.description}</p></div><span className="shrink-0 rounded-full bg-orange-50 px-2 py-1 text-[10px] font-bold uppercase text-brand-primary">{topic.difficulty}</span></div></button>)}</div></section>)}</div></div>;
}
